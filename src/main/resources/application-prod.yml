spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myguitarlib
    username: ----
    password: ----
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

